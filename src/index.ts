import { initServer } from "./app/app.server";

import { Bread } from "./app/bread/bread.class";

const port: number = 3000;
initServer(port);


// let bread: Bread;
// bread = new Bread();
// bread.emoji = '🍞';

// console.log('index.ts:12', bread.validateEmoji());


// console.log('index.ts:16', '🍞'.length);
